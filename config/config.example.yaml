# Example Digital Factory Configuration
# This is a sanitized example only. Do not use real credentials or paths.

kanban:
  auto_decompose: false
  auto_subscribe_on_create: true
  orchestrator_profile: digital-factory-orchestrator

# Example - DO NOT COPY REAL VALUES
# provider_api_key: "your-api-key-here"
# auth_token: "your-auth-token-here"
# database_url: "postgresql://user:pass@localhost/db"
# session_secret: "your-session-secret-here"
# cookie_settings: { ... }
# credential_store: { ... }
# private_endpoints: [ ... ]
# local_paths: { ... }